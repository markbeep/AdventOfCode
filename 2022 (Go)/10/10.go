package main

import (
	"aoc/util"
	"fmt"
)

func main() {
	f := util.ReadS("inp.txt", "\n")
	c := 0
	hash := map[string]bool{}

	for _, v := range f {
		fmt.Sscanf(v, "")

	}
	_ = f
	_ = hash
	fmt.Println(c)
}
